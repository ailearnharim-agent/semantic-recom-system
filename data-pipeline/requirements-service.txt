# Runtime dependencies for embed_service.py only -- deliberately excludes
# jupyter, matplotlib, pytest, datasets, psycopg2, pandas: those belong to
# the offline scripts (ingest/preprocess/build_index/EDA), not the service
# that actually gets containerized and runs in production.
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.8.2
sentence-transformers==3.0.1
torch==2.3.1
